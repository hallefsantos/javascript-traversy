const num1 = 100;
const num2 = 50;
let val;

// Simple math with numbers
val = num1 + num2; 
val = num1 * num2; 
val = num1 - num2; 
val = num1 / num2; 
val = num1 % num2;

// Math Object
val = Math.PI;
val = Math.E;
val = Math.round(2.4);
val = Math.ceil(2.4);
val = Math.floor(2.8);
val = Math.sqrt(4);
val = Math.abs(-3);
val = Math.pow(2,10);
val = Math.min(20,30,555,2,10);
val = Math.max(20,30,555,2,10);
val = Math.random();

val = Math.ceil(Math.random() * 100 + 1);

console.log(val);